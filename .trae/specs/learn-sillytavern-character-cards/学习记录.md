# SillyTavern 角色卡（Character Card）创建完整指南

> 一份系统性的角色卡制作学习记录，涵盖规范演进、字段详解、写作技巧、Token 管理与最佳实践。

---

## 目录

1. [一、什么是角色卡（Character Card）](#一什么是角色卡character-card)
2. [二、角色卡规范的版本演进](#二角色卡规范的版本演进)
3. [三、V2 核心字段详解](#三v2-核心字段详解)
4. [四、Token 预算管理](#四token-预算管理)
5. [五、角色描述写作格式](#五角色描述写作格式)
6. [六、角色卡质量评分标准与最佳实践](#六角色卡质量评分标准与最佳实践)
7. [七、Lorebook（角色知识库）](#七lorebook角色知识库)
8. [八、创建工具](#八创建工具)
9. [九、分享平台与导入导出](#九分享平台与导入导出)
10. [十、宏变量与占位符](#十宏变量与占位符)
11. [十一、MVU Zod 变量系统](#十一mvu-zod-变量系统)

---

## 一、什么是角色卡（Character Card）

### 1.1 角色卡的本质

角色卡是一种**特殊格式的 PNG 图片文件**，它在标准 PNG 图片中嵌入了结构化的 JSON 角色数据。简单来说，一张角色卡 = 一张图片 + 一套角色定义数据。当你双击一张角色卡图片时，你看到的是一张普通的角色立绘；但当你将它导入 SillyTavern 时，图片中隐藏的 JSON 数据会被解析出来，构成 AI 角色的完整设定。

### 1.2 技术原理

PNG（Portable Network Graphics）格式支持在文件中存储元数据，具体来说是利用 **PNG tEXt 数据块**。tEXt 块是一种键值对结构的文本元数据，原本用于存储版权信息、作者、软件名称等辅助信息。

角色卡的实现思路如下：

1. 将一个完整的 JSON 对象（包含角色的名称、描述、性格等）序列化为字符串
2. 对该字符串进行 **Base64 编码**（因为 tEXt 块只接受文本内容，Base64 确保二进制安全）
3. 将编码后的字符串作为一个 tEXt 块写入 PNG 文件
4. 读取时反向操作：从 tEXt 块提取 Base64 字符串 → 解码 → 得到 JSON 数据

```
┌─────────────────────────────────┐
│          PNG 文件结构            │
├─────────────────────────────────┤
│  PNG 签名 (8 bytes)             │
│  IHDR 块 (图像头)               │
│  ... 其他图像数据块 ...          │
│  tEXt 块 (关键字: "chara")       │  ← 角色数据存于此
│    └─ Base64 编码的 JSON 字符串  │
│  IEND 块 (图像结束)             │
└─────────────────────────────────┘
```

### 1.3 核心价值

| 价值 | 说明 |
|------|------|
| **一图一角色** | 一张图片即是一个完整的角色，用户直观看到角色外观 |
| **便于分享** | 图片格式通用，可以在任何平台传播、预览，不需要额外文件 |
| **跨平台兼容** | 基于开放标准 PNG + JSON，不依赖特定软件的数据格式 |
| **可扩展性** | JSON 结构允许不断新增字段，版本演进不会破坏兼容性 |

### 1.4 SillyTavern 与角色卡的关系

**SillyTavern** 是一个开源的 AI 角色扮演前端（前端界面），它将用户连接到各种 LLM 后端（如 OpenAI API、KoboldAI、TextGen WebUI、NovelAI 等），提供丰富的角色扮演功能。

角色卡是 SillyTavern 的**核心数据载体**：

- SillyTavern 通过解析角色卡的 JSON 数据来构建发送给 LLM 的提示词（Prompt）
- 角色卡中的 `description`、`personality`、`scenario` 等字段会被组装成"角色定义"，嵌入到每一次对话请求中
- 角色卡支持嵌入 **Lorebook（角色知识库）**，在对话中通过关键词触发注入额外的世界设定信息
- SillyTavern 支持导入/导出多种格式的角色卡，并提供内置编辑器来创建和修改角色

---

## 二、角色卡规范的版本演进

角色卡规范经历了三个主要版本的迭代，每个版本都在前一个版本的基础上增加了新的能力。

### 2.1 V1：基础版本

V1 是最早的角色卡格式，定义了 **6 个核心字段**：

| 字段 | 说明 |
|------|------|
| `name` | 角色名称 |
| `description` | 角色描述（外貌、背景等） |
| `personality` | 角色性格特质 |
| `scenario` | 互动场景设定 |
| `first_mes` | 角色的首条消息 |
| `mes_example` | 对话示例 |

V1 没有 `spec` 和 `spec_version` 字段，结构简单直观，为后续发展奠定了基础。早期角色卡领域的大量社区创作都基于 V1 格式。

### 2.2 V2：当前主流标准

**V2 是目前最广泛使用的版本**，它引入了明确的规范标识和大量新字段。

标识信息：
- `spec`: `"chara_card_v2"`
- `spec_version`: `"2.0"`
- PNG 中使用 tEXt 关键字 `"chara"`

**V2 新增字段**：

| 字段 | 说明 |
|------|------|
| `creator_notes` | 创作者备注，不参与提示词构建 |
| `system_prompt` | 角色专属系统提示词，支持 `{{original}}` 占位符 |
| `post_history_instructions` | 历史后指令（替代 UJB/Jailbreak） |
| `alternate_greetings` | 多个可选开场白（字符串数组） |
| `character_book` | 嵌入式 Lorebook，角色专属知识库 |
| `tags` | 分类标签数组 |
| `creator` | 作者名 |
| `character_version` | 角色版本号 |
| `extensions` | 扩展数据（默认 `{}`） |

### 2.3 V3：扩展格式

V3 在 V2 的基础上进一步扩展，支持更丰富的媒体资源。

标识信息：
- `spec`: `"chara_card_v3"`
- `spec_version`: `"3.0"`
- PNG 中使用 tEXt 关键字 `"ccv3"`

**V3 新增字段**：

| 字段 | 说明 |
|------|------|
| `group_only_greetings` | 群聊专用开场白 |
| `regex_scripts` | 正则脚本（用于自动化文本处理） |
| `assets` | 资产引用，支持多文件嵌入 |

**CharX 压缩包格式**：V3 引入了 `.charx` 格式，本质上是一个 **ZIP 压缩包**，内部可以包含：
- 一个或多个角色立绘图片
- 视频文件
- 其他媒体资产
- 主 JSON 数据文件

这使得角色卡不再局限于"一张图的 PNG"，而是可以成为一个完整的多媒体角色包。

### 2.4 版本对比总览

| 特性 | V1 | V2 | V3 |
|------|:--:|:--:|:--:|
| `spec` 标识 | ❌ | `chara_card_v2` | `chara_card_v3` |
| `spec_version` | ❌ | `2.0` | `3.0` |
| PNG tEXt 关键字 | 无规范 | `chara` | `ccv3` |
| 核心 6 字段 | ✅ | ✅ | ✅ |
| `system_prompt` | ❌ | ✅ | ✅ |
| `post_history_instructions` | ❌ | ✅ | ✅ |
| `character_book` | ❌ | ✅ | ✅ |
| `alternate_greetings` | ❌ | ✅ | ✅ |
| `tags` / `creator` / `character_version` | ❌ | ✅ | ✅ |
| `.charx` 压缩包 | ❌ | ❌ | ✅ |
| `group_only_greetings` | ❌ | ❌ | ✅ |
| 多图片/视频/资产 | ❌ | ❌ | ✅ |

---

## 三、V2 核心字段详解

> 以 V2 规范为主线，这是目前最主流且功能完备的版本。所有字段说明适用于实际创作。

### 3.1 name

- **类型**：`string`
- **Token 类别**：永久 Token
- **说明**：角色的名称。保持简洁，通常是一个词或短名。

AI 在生成消息时会使用这个名字来标注角色发言。过长的名称会浪费 Token 预算。

```json
"name": "Elara"
```

### 3.2 description

- **类型**：`string`
- **Token 类别**：永久 Token（始终在提示词中）
- **说明**：描述角色的**外貌特征、背景故事、世界观设定**。这是 AI 理解"这个角色是谁、长什么样、来自哪里"的核心依据。

description 在每次生成请求时都会被发送给 LLM，因此其 Token 消耗影响最大。应**只写外貌和背景设定**，不写内心性格（放在 personality 中）。

```json
"description": "Elara is a 24-year-old elven archer from the Silverwood Forest. She has long silver hair, emerald green eyes, and pointed ears. She wears a dark green cloak and carries an ornate longbow passed down through her family for generations. The Silverwood is a mystical forest on the edge of the human kingdom, home to the last remaining elf clans."
```

### 3.3 personality

- **类型**：`string`
- **Token 类别**：永久 Token（始终在提示词中）
- **说明**：描述角色的**内在性格、动机、价值观、言行风格**。告诉 AI "这个角色如何思考和行动"。

personality 应专注于内在特质，不要写外貌信息（那是 description 的职责）。可以包含说话方式、态度、情感倾向、行为模式等。

```json
"personality": "Elara is calm and observant, often speaking in a measured, thoughtful tone. She is fiercely protective of nature and her people, but carries a deep sadness from the gradual decline of the elf clans. She is slow to trust humans but will show warmth once trust is earned. Her humor is dry and subtle, and she prefers action over words."
```

### 3.4 scenario

- **类型**：`string`
- **Token 类别**：永久 Token（始终在提示词中）
- **说明**：设定**互动的场景上下文**，包括时间、地点、初始情境、相遇原因等。

scenario 帮助 AI 理解"当前发生了什么、角色和用户为什么在一起"。

```json
"scenario": "{{char}} and {{user}} meet at the edge of the Silverwood Forest at dusk. {{user}} is a wandering adventurer seeking the help of the elf clans. A recent attack from shadow creatures has left {{char}} suspicious of outsiders."
```

### 3.5 first_mes

- **类型**：`string`
- **Token 类别**：临时 Token（仅在聊天开始时发送一次）
- **说明**：**角色说的第一句话**。这是决定角色交流风格和语气的关键元素——用户对角色形成第一印象的核心时刻。

first_mes 不占用后续对话的 Token 预算，因此可以写得较为详细。一个好的 first_mes 应该：
- 展现角色的说话风格和语气
- 设定最初的互动情境
- 给用户提供明确的回应方向

```json
"first_mes": "*The soft rustle of leaves announces her presence before her voice does. Elara steps out from behind an ancient oak, her bow lowered but her eyes watchful.*\n\n\"You're a long way from the trade roads, stranger. Few wander this deep into the Silverwood without purpose. State yours, and choose your words carefully — the forest listens.\""
```

### 3.6 mes_example

- **类型**：`string`
- **Token 类别**：临时 Token（随上下文填满可能被推出）
- **说明**：**对话示例**，用于向 AI 展示角色的对话风格、节奏和行为模式。使用 `<START>` 作为每个示例块的分隔符。

mes_example 不占用永久 Token，但随着对话历史增长，它可能被从上下文中推出。因此应保持**精炼、有代表性**。

```json
"mes_example": "<START>\n{{user}}: What brings you to this part of the woods?\n{{char}}: *Elara nocks an arrow with practiced ease but doesn't draw.* \"Protection. The Silverwood has stood for millennia. I intend to see it stand for millennia more. The question is — what brings *you* here?\"\n<START>\n{{user}}: Can you teach me to shoot like that?\n{{char}}: *She lets out a soft, almost imperceptible laugh.* \"No one learns to shoot in a day, outsider. But if you're willing to bleed for it...\" *She gestures toward a nearby clearing.* \"We start at dawn.\""
```

### 3.7 creator_notes

- **类型**：`string`
- **Token 类别**：**不参与提示词构建**
- **说明**：创作者为自己或其他创作者写的备注。可以包含创作意图、灵感来源、使用建议等。LLM 永远不会看到此字段的内容。

```json
"creator_notes": "Elara is designed for a slow-burn fantasy adventure. She works best with GPT-4 class models. Lower temperature (0.7-0.9) recommended to maintain her measured tone."
```

### 3.8 system_prompt

- **类型**：`string`
- **Token 类别**：永久 Token（如果设置）
- **说明**：**角色专属的系统提示词**，用于覆盖或扩展用户的默认系统提示词。支持 `{{original}}` 占位符，该占位符会被替换为用户在 SillyTavern 设置中配置的默认系统提示词。

这使得角色创作者可以为角色定制行为约束，而不必依赖于用户的全局设置。

```json
"system_prompt": "{{original}}\n\n[IMPORTANT: You are roleplaying as Elara, an elven archer. You should never break character. Your responses should always include subtle physical descriptions of your movements and the forest environment around you.]"
```

### 3.9 post_history_instructions

- **类型**：`string`
- **Token 类别**：永久 Token（如果设置）
- **说明**：**历史后指令**（Post-History Instructions），在对话历史之后、AI 生成回复之前插入。这替代了早期的 UJB（Universal Jailbreak）概念。

同样支持 `{{original}}` 占位符。用于在 AI 生成回复的最后一刻施加行为指导。

```json
"post_history_instructions": "{{original}}\n\n[Remember: Elara does not use modern slang. Her speech is formal but not archaic. She often pauses to observe her surroundings mid-conversation. Do not speak for {{user}}.]"
```

### 3.10 alternate_greetings

- **类型**：`string[]`（字符串数组）
- **Token 类别**：临时 Token（仅选中的那条发送）
- **说明**：提供**多个可选的开场白**。用户在开始新对话时可以从这些候选中选择一个。

对于希望在不同场景中使用同一角色的情况非常有用（如：初次见面、熟悉后重逢、敌对状态等不同起始情境）。

```json
"alternate_greetings": [
  "*Elara stands at the edge of the Silverwood, bow in hand, watching you approach.* \"State your business, traveler.\"",
  "*In the warmth of a campfire, Elara looks across the flames at you.* \"You know, you're the first human I've trusted with my camp in fifty years.\"",
  "*Elara emerges from the shadows, arrow aimed at your heart.* \"I warned you never to return.\""
]
```

### 3.11 character_book

- **类型**：`object`（CharacterBook 结构）
- **Token 类别**：条件 Token（通过关键词触发注入）
- **说明**：**嵌入式 Lorebook**，角色专属的世界知识库。详细结构见[第七章](#七lorebook角色知识库)。

```json
"character_book": {
  "name": "Silverwood Lore",
  "description": "Lore entries for Elara's world",
  "scan_depth": 50,
  "token_budget": 500,
  "recursive_scanning": false,
  "entries": [
    {
      "keys": ["Silverwood", "forest"],
      "content": "The Silverwood is an ancient mystical forest...",
      "enabled": true,
      "insertion_order": 100
    }
  ]
}
```

### 3.12 tags

- **类型**：`string[]`（字符串数组）
- **Token 类别**：**不参与提示词构建**
- **说明**：用于分类和搜索的标签。用于在角色库中筛选角色，不会发送给 LLM。

```json
"tags": ["fantasy", "elf", "archer", "adventure", "slow-burn", "female"]
```

### 3.13 creator

- **类型**：`string`
- **Token 类别**：**不参与提示词构建**
- **说明**：角色卡作者的名字或化名。

```json
"creator": "AliceTheWriter"
```

### 3.14 character_version

- **类型**：`string`
- **Token 类别**：**不参与提示词构建**
- **说明**：角色的版本号，便于跟踪迭代更新。

```json
"character_version": "1.2.0"
```

### 3.15 extensions

- **类型**：`object`
- **Token 类别**：取决于扩展类型
- **说明**：扩展数据容器，默认值为 `{}`。用于存储第三方插件或未来扩展定义的额外数据。

```json
"extensions": {}
```

---

## 四、Token 预算管理

Token 预算是角色卡制作中**最容易被忽视但最重要的技术考量**。合理管理 Token 直接决定了 AI 角色扮演的质量和对话长度。

### 4.1 Token 分类

#### 永久 Token（Permanent Tokens）

这些字段的内容在**每次生成请求中都会被发送给 LLM**：

| 字段 | 说明 |
|------|------|
| `name` | 角色名称（通常 < 5 tokens） |
| `description` | 外貌、背景、世界设定 |
| `personality` | 内在性格、动机、言行风格 |
| `scenario` | 互动场景上下文 |
| `system_prompt`（如果设置） | 角色专属系统提示词 |
| `post_history_instructions`（如果设置） | 历史后指令 |

**关键影响**：永久 Token 越多，可用于保存聊天历史的 Token 越少，AI 的"记忆力"越差。

#### 临时 Token（Temporary Tokens）

这些字段仅在特定条件下发送，不持续占用预算：

| 字段 | 发送时机 |
|------|----------|
| `first_mes` | 仅在聊天开始时发送一次 |
| `mes_example` | 随上下文填充，对话增长后可能被逐出 |

### 4.2 推荐 Token 预算

| 区间 | Token 数 | 效果 |
|------|----------|------|
| **理想范围** | 500 ~ 2200 | 角色信息充足，AI 记忆良好 |
| **信息不足** | < 250 | 角色定义过于简单，AI 表现不稳定 |
| **信息过多** | > 2700 | AI "记忆力"显著下降，聊天历史保存更少 |
| **红色警报** | > 模型上下文一半 | 严重挤占对话历史空间 |

### 4.3 各流行模型上下文限制

| 模型 | 最大上下文 | 推荐永久 Token 上限 |
|------|-----------|---------------------|
| LLaMA 3 (8B) | 8,192 | ~2,000 |
| LLaMA 3 (70B) | 8,192 | ~2,000 |
| GPT-3.5 Turbo | 16,385 | ~4,000 |
| GPT-4 / GPT-4 Turbo | 128,000 | ~5,000 |
| Claude 3 (Haiku/Sonnet/Opus) | 200,000 | ~5,000 |
| Command R+ | 128,000 | ~5,000 |

> 永久 Token 上限为推荐值，实际可根据需求调整。但永远不要让角色定义 Token 超过模型总上下文的 **50%**。

### 4.4 Token 计算工具

在实际创作中，可以使用以下工具估算 Token：
- **OpenAI Tokenizer**（platform.openai.com/tokenizer）：精确计算 GPT 系列模型的 Token 数
- **SillyTavern 内置显示**：角色编辑界面通常会显示 Token 统计
- **tiktoken 库**：编程方式计算（Python）

---

## 五、角色描述写作格式

角色卡的 `description` 和 `personality` 字段可以使用多种写作格式。不同格式在 Token 效率、可读性和 AI 理解程度上有各自的特点。

### 5.1 自由文本（Free Text）

**最自然、最直观的格式**，使用自然语言段落描述角色。

```text
Elara is a 24-year-old elven archer from the Silverwood Forest. She has long silver hair, emerald green eyes, and wears a dark green cloak. She is calm and observant, fiercely protective of nature and her people. She speaks in a measured tone and is slow to trust humans.
```

**优点**：
- 自然流畅，易于人类阅读和编辑
- AI 理解自然语言非常出色
- 无学习成本

**缺点**：
- Token 效率较低（描述性词语占用较多 Token）
- 结构化程度低

### 5.2 W++ 格式

**结构化属性标签格式**，使用方括号包裹属性类别和值。格式为 `[Category(attribute1, attribute2, ...)]`。

```text
[Appearance(long silver hair, emerald green eyes, pointed ears, dark green cloak, ornate longbow)]
[Clothing(dark green cloak, leather bracers, soft boots)]
[Personality(calm, observant, protective, slow to trust, dry humor)]
[Motivation(protect the Silverwood, preserve elf heritage, find lost clan members)]
[Speech(measured, thoughtful, formal but not archaic, pauses often)]
[Background(24 years old, Silverwood Forest native, last surviving member of her scout group)]
```

**优点**：
- Token 效率高（紧凑的键值结构）
- 高度结构化，便于 AI 解析

**缺点**：
- 格式较机械，缺乏温度
- 需要学习 W++ 规范

### 5.3 Boostyle 格式

**关键词/属性列表格式**，极度紧凑，使用少量词汇密集描述。通常用加号连接或用逗号分隔。

```text
Elara[elf + female + 24yo + archer + Silverwood + silver hair + emerald eyes + calm + observant + protective + dry humor + measured speech + formal + slow to trust + forest guardian + longbow + dark green cloak]
```

或者更简洁的纯关键词列表：

```text
elf, female, 24yo, archer, Silverwood, silver hair, emerald eyes, calm, observant, protective, dry humor, measured speech, formal, slow to trust
```

**优点**：
- Token 效率极高
- 适用于上下文窗口较小的模型

**缺点**：
- 可读性差
- AI 可能无法从离散关键词中构建连贯的角色形象

### 5.4 Ali:Chat 格式

Ali:Chat 是一种哲学层面的写作方法论，其**核心思想是通过对话示例来表达角色特质，而非在 description 中平铺直述**。

#### 核心理念

传统方法是在 description 中写"Iron Bull 很大声且粗鲁"，Ali:Chat 则认为应该**通过对话展示这一点**。

#### 四个关键原则

**1. description 中包含对话**

在 description 中嵌入角色的对话片段（非正式对话，而是角色心理/态度的展现）：

```text
Elara is an elf archer. {{char}} sometimes thinks, "These humans trample through the forest like deaf bears — how did their species survive this long?"
```

这比写 `Elara dislikes humans who disrespect nature` 更生动。

**2. first_mes 详细设定场景**

first_mes 不仅是问候，而是**建立整个场景的情绪基调、空间感和互动理由**：

```text
*The Silverwood at dusk is a cathedral of silence, broken only by the distant hoot of an owl. Elara has been tracking {{user}} for the past hour, her footsteps silent on the moss-covered earth. Finally, she steps into view, positioning herself between {{user}} and the deeper woods.*

"You've been wandering in circles for an hour, outsider. The forest doesn't want you here yet. Why should I feel differently?"
```

**3. mes_example 推动对话方向**

mes_example 不只是展示说话风格，而是**示范角色如何推进互动**：

```text
<START>
{{user}}: I'm lost. Can you help me find the road?
{{char}}: *Elara studies {{user}} for a long moment, then gestures north.* "The road is that way." *She doesn't move.* "But you didn't come to the Silverwood just to find a road, did you? No one does."
```

**4. 不重复信息**

如果 personality 已经写了"calm"，description 不需要再写一遍。每个字段做自己该做的事，避免冗余浪费 Token。

---

## 六、角色卡质量评分标准与最佳实践

### 6.1 五大评分维度（各 0–20 分）

| 维度 | 满分 | 评估内容 |
|------|:----:|----------|
| **1. 字段适配性与结构** | 20 | 是否正确使用各字段，不混用职责 |
| **2. 角色连贯性与一致性** | 20 | description、personality、scenario、first_mes 是否描述同一角色 |
| **3. 角色扮演实用性与沉浸感** | 20 | first_mes 是否设定语调、场景和互动理由 |
| **4. 创意与原创性** | 20 | 构思是否有新意，避免陈词滥调 |
| **5. 打磨与技术要求** | 20 | 语法清晰，格式可读，正确使用 `{{char}}`/`{{user}}` 占位符 |
| **总分** | **100** | *(注：原始评分表有 5 分的额外加分空间，满分实为 105)* |

### 6.2 等级划分

| 等级 | 分数区间 | 含义 |
|:----:|----------|------|
| **S** | 90–105 | 卓越：角色在所有方面均表现出色 |
| **A** | 80–89 | 优秀：高质量角色，体验很好 |
| **B** | 60–79 | 良好：可用，但有几个明显改进空间 |
| **C** | 40–59 | 及格：有基本功能但体验平平 |
| **D** | 30–39 | 差：存在重大问题 |
| **F** | 0–29 | 极差：基本不可用 |

### 6.3 常见错误

| 错误类型 | 表现 | 正确做法 |
|----------|------|----------|
| **字段空置或误用** | personality 为空，或把性格写进了 description | 每个字段各司其职（见下方"字段内容分配建议"） |
| **缺少 first_mes** | 没有 first_mes，或仅写 `"Hi"`、`"Hello"` | 撰写有场景感、展现性格的开场白 |
| **无 mes_example** | mes_example 字段为空 | 至少提供 2–3 个交错的对话示例 |
| **单个字段过度填充** | description 写了 5000 字 | 控制 Token，把详细信息放入 Lorebook |
| **性格矛盾** | personality 写"温柔"，first_mes 写攻击性内容 | 所有字段保持角色一致 |
| **Token 总量失衡** | 永久 Token 超过 3000 | 精简到 500–2200 的理想范围 |

### 6.4 字段内容分配建议

明确每个字段应该写什么、**不应该写什么**，是避免常见错误的关键：

| 字段 | 应该写 | 不应该写 |
|------|--------|----------|
| **description** | 外貌特征、背景故事、世界设定 | 内心情感、性格特质（应放在 personality） |
| **personality** | 内在特质、社交风格、动机、价值观 | 外貌描述（应放在 description） |
| **scenario** | 时间、地点、相遇原因、初始情势 | 详细的角色信息（应在其他字段） |
| **first_mes** | 强有力的开场白，展现角色风格和场景 | 平淡的问候语如 "Hello" |
| **mes_example** | 短小精悍的对话片段，用 `<START>` 分隔 | 长篇叙事（应保持示例精简） |

### 6.5 最佳实践清单

- ✅ 使用 `{{char}}` 和 `{{user}}` 占位符代替硬编码的角色名和用户名
- ✅ 每个字段只做自己的事，不混用职责
- ✅ first_mes 中融入动作和场景描述（用星号包裹 `*like this*`）
- ✅ mes_example 提供 2–5 个简短但有代表性的对话片段
- ✅ 将大量世界设定细节放入 Lorebook 而非 description
- ✅ 定期检查 Token 总数，保持在 500–2200 的理想范围
- ✅ 添加合理数量的 tags，帮助用户搜索
- ✅ 为不同起始场景提供 alternate_greetings

---

## 七、Lorebook（角色知识库）

### 7.1 概念

**Lorebook**（也称为 World Book 或 Character Book）是一个嵌入在角色卡中的**世界设定知识库**。它由多个条目（Entry）组成，每个条目定义了一组触发关键词和相关内容。当对话中出现关键词时，对应的内容会被动态注入到 LLM 的上下文中。

与永久 Token 不同，Lorebook 的内容**仅在触发时才注入**，因此它是管理大量世界设定细节的高效方式。

### 7.2 条目（Entry）结构

每个 Lorebook 条目的基本结构：

```json
{
  "keys": ["Silverwood", "forest", "ancient woods"],
  "content": "The Silverwood is an ancient mystical forest on the edge of the human kingdom...",
  "enabled": true,
  "insertion_order": 100
}
```

| 属性 | 类型 | 说明 |
|------|------|------|
| `keys` | `string[]` | 触发关键词数组。当对话中出现任一关键词时，条目被注入 |
| `content` | `string` | 注入到上下文中的具体内容 |
| `enabled` | `boolean` | 条目是否启用 |
| `insertion_order` | `number` | 插入顺序编号（决定多个条目在上下文中的排列顺序） |

### 7.3 高级属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `selective` | `boolean` | 双重关键词触发 —— 条目的 keys 需要对话中**同时出现至少两个**关键词才触发 |
| `constant` | `boolean` | 始终注入 —— 无论对话内容如何，该条目总是被包含在上下文中 |
| `recursive_scanning` | `boolean` | 递归触发 —— 条目注入的内容本身也可以触发其他条目 |
| `position` | `string` | 注入位置 —— `"before_char"`（角色定义之前）或 `"after_char"`（角色定义之后） |
| `priority` | `number` | 优先级 —— 当 Token 预算不足时，**低优先级的条目先被丢弃** |

### 7.4 CharacterBook 整体结构

```json
"character_book": {
  "name": "Silverwood Encyclopedia",
  "description": "Comprehensive lore for Elara's world",
  "scan_depth": 50,
  "token_budget": 500,
  "recursive_scanning": false,
  "entries": [
    {
      "keys": ["Silverwood", "forest"],
      "content": "The Silverwood is an ancient mystical forest covering the northeastern border of the human kingdom. The trees here are thousands of years old, their bark shimmering with a faint silver glow under moonlight. It is home to the last remaining elf clans.",
      "enabled": true,
      "insertion_order": 100,
      "priority": 10
    },
    {
      "keys": ["elf clans", "elves", "clans"],
      "content": "The elf clans of the Silverwood are divided into three major houses: House Aldari (archers and scouts), House Mithrendil (scholars and mages), and House Thornwood (warriors and guardians). Elara belongs to House Aldari.",
      "enabled": true,
      "insertion_order": 200,
      "priority": 8
    },
    {
      "keys": ["shadow creatures", "darkness", "attack"],
      "content": "Shadow creatures are malevolent beings that emerged from the deep caverns beneath the Silverwood fifty years ago. They are vulnerable to light and enchanted silver. The elf clans have been at war with them ever since, slowly losing ground.",
      "enabled": true,
      "insertion_order": 300,
      "priority": 7
    }
  ]
}
```

| CharacterBook 属性 | 说明 |
|-------------------|------|
| `name` | 知识库名称 |
| `description` | 知识库描述（不发送给 LLM） |
| `scan_depth` | 扫描对话历史的深度（最近 N 条消息） |
| `token_budget` | Lorebook 注入内容的最大 Token 预算 |
| `recursive_scanning` | 是否启用递归触发 |
| `entries` | Lorebook 条目数组 |

### 7.5 使用策略

- **将细节从 description 移入 Lorebook**：如果某个世界设定细节不会在每一轮对话中都用到，它就应该在 Lorebook 中
- **关键词设计要自然**：触发关键词应该是在对话中自然出现的词，而非生僻的标记
- **控制条目大小**：每个条目应简短精炼（50–150 tokens），不要写成长篇大论
- **合理设置优先级**：核心设定给高优先级，边缘细节给低优先级
- **token_budget 要留有空间**：不要设得过高，确保有足够的 Token 留给对话

---

## 八、创建工具

### 8.1 SillyTavern 内置编辑器

SillyTavern 自带的角色管理工具，是最直接的角色创建方式。

**路径**：菜单栏 → 角色管理 → 创建新角色

功能包括：
- 完整的 V2 字段编辑
- 内嵌 Lorebook 编辑器
- Token 统计显示
- 实时预览角色信息
- 导入/导出 PNG 角色卡

### 8.2 Chara Snap

**网址**：[charasnap.com](https://charasnap.com)

一个免费的浏览器端角色卡编辑器，特点：

| 特性 | 说明 |
|------|------|
| **运行方式** | 100% 客户端运行（数据不上传服务器） |
| **是否需要注册** | 不需要 |
| **支持版本** | V2 和 V3 |
| **功能** | 可视化编辑所有字段、Lorebook、多表情图片、CharX 导出 |
| **适用场景** | 快速创建、不想安装 SillyTavern 时使用 |

### 8.3 MegaNova Studio

云端角色工作室，提供更高级的编辑功能：

- 支持 CCv2 和 CCv3 格式导出
- 云端存储（需注册账户）
- 协作编辑功能
- 更丰富的媒体管理（V3 的 CharX 支持）

### 8.4 Character Card Extractor

一个**反向工具**：从已有的 PNG 角色卡中提取 JSON 数据。当你拿到一张角色卡但想查看或修改其底层数据时使用。

工作原理：解析 PNG 文件的 tEXt 块 → 提取 Base64 数据 → 解码为可读的 JSON。

常用提取工具：
- Chara Snap 内置的导入解析功能
- 在线 PNG 元数据查看器
- 命令行工具（如 `exiftool`）

---

## 九、分享平台与导入导出

### 9.1 主要分享平台

| 平台 | 说明 |
|------|------|
| **Chub.ai** | 目前最大的角色卡分享社区，支持搜索、分类、评分 |
| **CharacterHub** | 专注于角色卡分享的平台，社区规模较大 |
| **AICharacterCards.com** | AI 角色卡聚合网站 |
| **Janitor AI** | 角色扮演平台，支持角色卡格式，拥有活跃的创作者社区 |
| **Reddit** (r/CharacterAI 等) | 社区驱动的分享，非专门平台但有大量讨论 |
| **Discord** | 多个 SillyTavern 和角色卡相关的 Discord 服务器 |

### 9.2 导入角色卡

在 SillyTavern 中导入角色卡：

1. 打开 SillyTavern
2. 点击菜单栏 → 角色管理
3. 点击"导入角色"
4. 选择 PNG 文件或 JSON 文件
5. 系统自动解析并加载角色

支持导入的格式：
- **PNG** 角色卡（含嵌入的 JSON 元数据）
- **JSON** 文件（纯角色数据，无图片）
- **CharX** 压缩包（V3 格式，`.charx` 文件）

### 9.3 导出角色卡

SillyTavern 支持多种导出格式：

| 格式 | 说明 | 适用版本 |
|------|------|:--------:|
| **PNG 角色卡** | 图片中嵌入 JSON 元数据 | V1/V2/V3 |
| **JSON** | 纯角色数据文件 | V1/V2/V3 |
| **CharX 压缩包** | ZIP 包，含图片/视频/资产 | V3 |

### 9.4 常见导入失败原因

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| **图片内无嵌入定义** | PNG 文件被某些程序（社交媒体压缩、图片编辑器）处理时抹除了 tEXt 元数据 | 从原始来源重新下载，或使用未压缩的传输方式 |
| **WEBP 伪装的 PNG** | 文件实际上是 WEBP 格式但扩展名为 .png | 检查文件的实际格式；角色卡必须是真正的 PNG |
| **版本不兼容** | 角色卡使用了更新版本规范但目标的 SillyTavern 版本过旧 | 更新 SillyTavern 到最新版本 |
| **编码损坏** | Base64 数据在传输中损坏 | 重新导出/下载角色卡 |

> **最佳做法**：分享角色卡时使用**原始 PNG 文件**，避免通过会压缩图片的平台（如某些社交媒体、即时通讯工具）传输。

---

## 十、宏变量与占位符

宏变量（Macros）是角色卡中使用的特殊占位符，它们在发送给 LLM 之前会被替换为实际值。掌握宏变量是写出通用、可复用角色卡的关键。

### 10.1 名称类宏

| 宏 | 替代写法 | 替换为 |
|-----|----------|--------|
| `{{char}}` | `<BOT>` | 角色的 `name` 字段值 |
| `{{user}}` | `<USER>` | 用户在 SillyTavern 中设置的用户名 |

```text
// 角色卡中写：
{{char}} looks at {{user}} with curiosity.

// 实际发送给 LLM：
Elara looks at 小明 with curiosity.
```

> **最佳实践**：始终使用 `{{char}}` 和 `{{user}}` 代替硬编码名称。这样角色卡可以在不同用户间复用，而不需要每个用户手动修改名称。

### 10.2 占位替换类宏

| 宏 | 替换为 |
|-----|--------|
| `{{original}}` | 用户在 SillyTavern 设置中配置的原始系统提示词 / Jailbreak 文本 |
| `{{charPrompt}}` | 角色的主要提示覆盖（Character Prompt Override） |
| `{{charJailbreak}}` | 角色的 Jailbreak 覆盖 |

`{{original}}` 是最重要的占位符宏，通常用于 `system_prompt` 和 `post_history_instructions` 字段：

```json
"system_prompt": "{{original}}\n\n[Always stay in character as Elara.]"
```

这样设置后，角色的专属指令会**追加**在用户的默认系统提示词之后，而不是完全覆盖它。

### 10.3 内容类宏

| 宏 | 替换为 |
|-----|--------|
| `{{description}}` | 角色的 `description` 字段内容 |
| `{{personality}}` | 角色的 `personality` 字段内容 |
| `{{scenario}}` | 角色的 `scenario` 字段内容 |

这些宏允许在提示词模板中灵活引用角色字段的内容。它们通常在提示词模板中使用，而不是在角色卡字段本身中使用。

### 10.4 格式化宏

| 宏 | 效果 |
|-----|------|
| `{{newline}}` | 插入一个换行符（`\n`） |
| `{{trim}}` | 修剪（删除）包围此宏的换行符，防止产生多余空行 |
| `{{noop}}` | 无操作，回退为空字符串。用于占位或注释目的 |

```text
// 使用示例：
第一段文字 {{trim}}
{{newline}}
第二段文字

// 效果：{{trim}} 消除了前后的多余换行，得到一个整洁的换行连接。
```

### 10.5 宏变量使用注意事项

- ✅ 宏变量**区分大小写**，`{{char}}` 和 `{{CHAR}}` 是不同的
- ✅ 在 `first_mes` 和 `mes_example` 中必须使用宏变量而非硬编码名称
- ✅ `{{original}}` 只能用于 `system_prompt` 和 `post_history_instructions`
- ✅ `{{trim}}` 对于控制提示词格式和节省 Token（减少不必要的空行）非常有用
- ⚠️ 宏变量**不会递归展开**——一个宏变量的值中如果包含另一个宏变量的语法，不会被再次替换

---

## 十一、MVU Zod 变量系统

MVU Zod 变量系统是 SillyTavern 中用于制作**游戏化角色卡**和**管理剧情状态**的核心工具。它允许创作者定义结构化的变量（通过 Zod Schema），让 AI 在对话中动态更新这些变量（如好感度、物品栏、任务进度等），并通过世界书条目选择性发送提示词来实现复杂的分阶段剧情。

> MVU Zod 是 MVU 和 MVU Beta 的继承者，语法更简洁、功能更强大。以下内容基于 stagedog.github.io 教程整理。

### 11.1 为什么需要变量系统

| 用途 | 说明 |
|------|------|
| **记录剧情最新状态** | 好感度、物品栏、任务进度等数值始终对 AI 可见，AI 不再"遗忘" |
| **制作游戏化角色卡** | 商城积分、技能升级、大地图、战斗系统等复杂机制 |
| **分阶段发送提示词** | 分阶段人设、主线剧情、突发事件 —— 只在需要时注入对应提示词，大幅节省 Token |
| **替代传统状态栏** | 传统"每层都输出完整文本"的状态栏易掉格式且费 Token；变量系统由 AI 在内部维护数值，界面化展示 |

### 11.2 系统组件总览

MVU Zod 变量系统由以下 8 个组件构成：

| 组件 | 说明 | 位置 |
|------|------|------|
| **变量结构脚本** | Zod Schema 定义变量类型与约束 | 酒馆助手 → 角色脚本 |
| **[initvar] 初始变量** | 开局变量初始值（YAML 格式） | 世界书禁用条目 |
| **变量列表** | 实时展示当前变量值给 AI | 世界书蓝灯条目 D0/D1 |
| **[mvu_update] 变量更新规则** | 定义变量何时应被修改 | 世界书蓝灯条目 |
| **[mvu_update] 变量输出格式** | 指导 AI 以 JSON Patch 输出更新 | 世界书蓝灯条目 |
| **酒馆正则** | 隐藏 UpdateVariable、处理占位符 | 酒馆右上角 → 正则 |
| **酒馆助手脚本（可选）** | 监听 MVU 事件，后台控制变量 | 酒馆助手 → 脚本 |
| **前端界面（可选）** | HTML 状态栏 / 交互界面 | 正则替换 StatusPlaceHolderImpl |

### 11.3 变量结构脚本（Zod Schema）

变量结构使用 **Zod 4** 在酒馆助手的角色脚本中定义。

#### 文件骨架

```js
import { registerMvuSchema } from '..\\.\\.\\scripts\\extensions\\shared\\mvu_v2.js';

const Schema = z.object({
    category: z.object({        // 不需要 macro 函数包装
        hp: z.number().int().min(0).max(100).describe('角色血量'),
        maxHp: z.number().int().min(1).max(100).default(100),
        // ...
    }),
    // ...
});

$(() => registerMvuSchema(Schema));
```

> **注意**：`$(() => ...)` 是 jQuery 的文档就绪写法，酒馆助手用它来确保 DOM 加载完成后再注册 Schema。`registerMvuSchema` 路径请根据实际文件位置调整。

#### Zod 4 核心语法

| 语法 | 说明 | 示例 |
|------|------|------|
| `z.number()` | 数字类型 | `z.number().int().min(0).max(100)` |
| `z.string()` | 字符串类型 | `z.string().describe('角色心情')` |
| `z.boolean()` | 布尔类型 | `z.boolean().default(false)` |
| `z.enum([...])` | 枚举类型 | `z.enum(['友好', '中立', '敌对'])` |
| `.describe('...')` | 为字段添加描述（AI 可见） | `.describe('角色对主角的好感度')` |
| `.default(value)` | 默认值 | `.default(100)` |
| `.prefall(value)` | 预回落值（默认值 + 乐观更新） | `.prefall(0)` |
| `.transform(fn)` | 值变换（用于 clamp 等） | `.transform(v => clamp(v, 0, 100))` |
| `z.record(z.type())` | 字典/映射类型 | `z.record(z.number())` |

#### 编写要点

1. **数值范围用 `transform` + `clamp`**：而非仅用 `.min().max()`，后者在越界时直接报错中断，而 `clamp` 会截断到合法范围
   ```js
   const clamp = (value, min, max) => Math.min(Math.max(value, min), max);
   // ...
   favorability: z.number().int().prefall(0)
       .transform(v => clamp(v, -100, 100))
       .describe('好感度，范围 -100 到 100')
   ```

2. **使用 `prefall` 而非 `default`**：`prefall` 是 MVU 特有的"预回落值"，支持乐观更新（先应用变更再校验），在并发更新场景更安全

3. **`record` 优于数组**：Zod 对数组的元素级约束有限，优先使用 `z.record(z.string())` 等字典结构

4. **幂等性**：确保多次应用相同的 JSON Patch 操作得到相同结果

5. **变量命名规则**：不能使用宏作为变量名。例如用 `"主角"` 而非 `"{{user}}"`，因为宏在变量路径中不会被替换

#### 完整代码示例（白娅）

```js
import { registerMvuSchema } from '..\\.\\.\\scripts\\extensions\\shared\\mvu_v2.js';

const clamp = (value, min, max) => Math.min(Math.max(value, min), max);

const Schema = z.object({
    关系: z.object({
        好感度: z.number().int().prefall(0)
            .transform(v => clamp(v, -100, 100))
            .describe('白娅的好感度，-100极度厌恶，0中立，100深爱'),
    }),
    角色状态: z.object({
        心情: z.string().prefall('平常').describe('当前情绪状态'),
        位置: z.string().prefall('教室内').describe('白娅当前所在位置'),
    }),
    剧情: z.object({
        阶段: z.string().prefall('日常').describe('当前剧情阶段'),
        天数: z.number().int().prefall(1)
            .transform(v => clamp(v, 1, 365))
            .describe('剧情经过的天数'),
    }),
});

$(() => registerMvuSchema(Schema));
```

### 11.4 变量初始化

#### [initvar] 条目规则

- 世界书条目名称中必须包含 `[initvar]`
- 条目**必须处于禁用状态**（红灯），否则会被当作普通条目注入上下文
- 条目内容使用 **YAML 格式**，结构与 Zod Schema 对应
- 可写多个 [initvar] 条目，它们会被合并

```yaml
关系:
  好感度: 0
角色状态:
  心情: 平常
  位置: 教室内
剧情:
  阶段: 日常
  天数: 1
```

#### `<initvar>` 开局消息覆盖

在 `first_mes`（或 any greeting）中，可以使用 `<initvar>` XML 标签来覆盖初始值：

```xml
<initvar>
关系:
  好感度: 10
剧情:
  阶段: 初次见面
</initvar>
```

#### 优先级规则

| 情况 | 行为 |
|------|------|
| 有 `<initvar>` | **完全覆盖** `[initvar]` 的值（增量 JSON Patch，非全量替换整个变量树） |
| 无 `<initvar>` | 使用 `[initvar]` 的默认初始值 |

> **全量方案 vs 增量方案**：推荐使用 `<initvar>` 的 JSON Patch 增量方式在开场消息中微调部分变量（如指定起始好感度），而非在 [initvar] 中写死所有值。这样同一张角色卡的不同开局（alternate_greetings）可以有不同的初始变量值。

### 11.5 三大变量提示词

#### 一、变量列表

变量列表是让 AI "看到"当前所有变量值的世界书条目。

**固定模板**：

```xml
<status_current_variable>
{{format_message_variable::stat_data}}
</status_current_variable>
```

**关键规则**：

- **必须放在 D0 或 D1**（深度 0 或 1）：如果放在更深层，AI 可能误判数值为旧数据，导致"数值没变却以为变了"、"数值变了却以为没变"
- `format_message_variable` 宏会将整个变量树格式化为人类可读的文本，例如：`关系.好感度: 50`
- 可拆分为多个条目（如好感度栏、物品栏、任务栏），用**绿灯**选择性发送
- **进阶**：手动选择展示子集路径，减少 Token 消耗

```
{{format_message_variable::stat_data::关系}}
{{format_message_variable::stat_data::物品}}
```

#### 二、变量更新规则

指导 AI **何时应该更新哪些变量**。

**字段结构**：

```yaml
type: number          # 变量类型：number / string / boolean / enum
range: [-100, 100]    # 数值范围
format: int           # 格式：int / float / string
check: |              # 核心：更新触发条件（AI 用于判断何时修改）
  - 白娅对主角产生好感时，增加 2~8 点
  - 主角做出让她厌恶的行为时，减少 5~15 点
```

**编写技巧**：

| 技巧 | 说明 |
|------|------|
| **自明变量省略** | 如 `位置`、`心情` 等从对话中显然可知的变量，无需写复杂的 check |
| **同类变量合并** | 多个同类变量共用一个更新规则条目 |
| **动态键用索引签名** | 如 `物品.{物品名}.数量` 使用 Record 类型，check 中描述增减规则即可 |

条目名称前缀 `[mvu_update]` 使其适配两种更新方式（详见 11.11）。

更新规则条目可以放在角色定义前、后等不占注意力的位置 —— CoT（思维链）会"召回"这些规则。

#### 三、变量输出格式

指导 AI **如何将变量更新输出为可解析的格式**（JSON Patch）。

**结构**：

```
rule: |
  当 ${变量》发生 ${变化》时，使用以下格式输出更新
format: |
  ```json
  [
    {"op": "replace", "path": "/关系/好感度", "value": 55},
    {"op": "delta", "path": "/关系/好感度", "value": 5}
  ]
  ```
  - ${描述每个操作的含义》
  - $(要求：必须使用 delta 而非 replace 做增减》
  - ...
```

**format 中的特殊语法**：

| 语法 | 含义 |
|------|------|
| `${描述}` | 描述占位，AI 会填入具体值 |
| `$(要求)` | 给 AI 的强制性要求 |
| `...` | 补充说明 |

**JSON Patch 6 种操作**：

| 操作 | 说明 | 示例 |
|------|------|------|
| `replace` | 替换为绝对值 | `{"op": "replace", "path": "/关系/好感度", "value": 55}` |
| `delta` | 增减（正数增加，负数减少） | `{"op": "delta", "path": "/关系/好感度", "value": 5}` |
| `insert` | 插入新元素 | `{"op": "insert", "path": "/物品/药水", "value": {"数量": 3}}` |
| `remove` | 删除元素 | `{"op": "remove", "path": "/物品/过期钥匙"}` |
| `move` | 移动元素 | `{"op": "move", "path": "/背包/药水", "from": "/物品/药水"}` |

**Analysis 思维链三步分析**：

1. **时间分析**：判断当前对话经过了多长时间（几分钟？几天？）
2. **允许剧烈变动**：告知 AI 在时间跨度大或重大事件时，变量可以大幅变化
3. **回顾 check**：提醒 AI 回忆变量更新规则中定义的触发条件

**路径规则**：

- 路径以 `/` 开头
- **不需要 `stat_data` 前缀**：AI 输出 `/关系/好感度` 而非 `/stat_data/关系/好感度`
- 变量输出格式强调条目：放在最后确保 AI 在输出回复结尾才处理更新

### 11.6 提示词模板与 EJS 动态提示词

EJS（Embedded JavaScript）允许在世界书条目中使用 JavaScript 代码动态生成提示词，实现**条件化的人设发送**。

#### 核心语法

| 语法 | 说明 |
|------|------|
| `<%_ code _%>` | 包裹 JavaScript 代码，不输出 |
| `<%= value %>` | 输出转义后的值 |
| `<%- value %>` | 输出未转义的值 |
| `<>` 外的内容 | 普通提示词文本 |

#### 条件判断

```ejs
<%_ if (getvar('剧情.阶段') === '初遇') { _%>
白娅对你保持着礼貌但疏远的态度，说话时总是带着几分审视。
<%_ } else if (getvar('剧情.阶段') === '友人') { _%>
白娅已经习惯你的存在，会主动与你分享日常琐事，偶尔露出俏皮的一面。
<%_ } else { _%>
白娅在你面前已经完全放下防备，眼神中总藏着一丝温柔。
<%_ } _%>
```

#### 关键 API

| API | 说明 |
|-----|------|
| `getvar('路径')` | 获取变量值，路径与 `format_message_variable` 相同 |
| `print('文本')` | 在代码块内输出提示词文本 |
| `getwi('条目名')` | 获取其他世界书条目的内容 |
| `matchChatMessages('正则')` | 模拟绿灯功能 —— 匹配聊天消息，返回匹配到的内容数组 |

#### `@@preprocessing` 指令（1.13.4+）

在条目标题中加入 `@@preprocessing` 可以让 EJS **在世界书激活之前**就完成处理：

```
条目名@@preprocessing
```

这意味着即使该条目本身是红灯（禁用），其 EJS 代码也会被预先执行。常用于通过 `getwi()` 动态组装其他条目的内容。

#### 实用示例：好感度阶段分层的动态人设

```ejs
<%_
const favorability = getvar('关系.好感度');
if (favorability >= 80) {
    print('白娅已深深爱上你。她会在独处时主动靠近，说话声音不自觉地放轻，偶尔脸红。');
} else if (favorability >= 40) {
    print('白娅对你颇有好感。她愿意与你共度课余时间，谈话中会不经意地微笑。');
} else if (favorability >= 0) {
    print('白娅对你的态度中立偏友善。她不会刻意回避你，但也不会主动亲近。');
} else {
    print('白娅对你心存芥蒂。她会尽量避免与你单独相处，回答简短而冷淡。');
}
_%>
```

#### 让 AI 编写 EJS 的技巧

可以向 AI 描述需求，让 AI 生成 EJS 代码。例如：

> "请根据以下变量结构，写一段 EJS 代码：当 `好感度 >= 60` 时发送亲密版人设，否则发送普通版人设。"

### 11.7 酒馆正则

正则用于处理 AI 输出中的变量更新命令和界面占位符。

#### 三个必须正则

| 正则 | 类型 | 用途 |
|------|------|------|
| 去除变量更新 | promptOnly | 隐藏 AI 输出中的 `UpdateVariable` JSON Patch，不让用户看到 |
| 界面占位符 | promptOnly | 隐藏 `StatusPlaceHolderImpl` 占位符 |
| 状态栏美化 | markdownOnly | 将状态占位符替换为实际的 HTML 界面 |

#### 去除变量更新正则（promptOnly）

```
/```json\s*\n?\s*\[[\s\S]*?\]\s*\n?\s*```/g
```
作用：匹配并隐藏 AI 输出的 JSON Patch 代码块。

#### 界面占位符正则（promptOnly）

```
/StatusPlaceHolderImpl/g
```
作用：隐藏占位符文本，防止在后续上下文中出现。

#### 状态栏美化方案

**美化版**（markdownOnly 替换为 HTML）：

将 `StatusPlaceHolderImpl` 替换为一段完整的 HTML + CSS 界面，显示角色状态。

**折叠版**（节省空间）：

```html
<details>
<summary>📊 角色状态</summary>
<!-- 变量显示内容 -->
</details>
```

**仅提示版**（最省 Token）：

直接将占位符替换为 `{{format_message_variable::stat_data}}` 宏输出的纯文本。

**minDepth 参数**：设置 `minDepth` 保留最近 N 层消息的界面，更早的消息中界面被移除，节省上下文。

### 11.8 状态栏界面

#### StatusPlaceHolderImpl 占位符机制

AI 在输出回复时，会在文末自动插入 `StatusPlaceHolderImpl` 文本。酒馆正则的 `markdownOnly` 规则将其替换为实际的界面代码。

#### 纯文本版

无需前端代码，直接在正则中将占位符替换为 `{{format_message_variable::stat_data}}` 宏。

#### HTML 美化版

结合内联 CSS，将变量格式化为美观的状态面板：

```html
<div style="background:#1a1a2e; color:#e0e0e0; border-radius:8px; padding:12px; margin:8px 0; font-size:13px;">
  <div style="font-weight:bold; margin-bottom:8px;">📊 角色状态</div>
  {{format_message_variable::关系.好感度::白娅好感度：{:value} }}
  {{format_message_variable::角色状态.心情::心情：{:value} }}
  {{format_message_variable::角色状态.位置::位置：{:value} }}
</div>
```

`format_message_variable` 的格式参数：`{路径::标题::模板}`，`{:value}` 会被替换为实际值。

#### 前端交互版

使用 JavaScript 驱动，监听 MVU 事件实时更新界面：

```html
<div id="status-panel"></div>
<script>
(async () => {
    await window.waitGlobalInitialized();
    const update = () => {
        const vars = window.getAllVariables();
        document.getElementById('status-panel').innerHTML = `
            ❤️ 好感度：${vars['关系']?.['好感度'] ?? '--'}
            😊 心情：${vars['角色状态']?.['心情'] ?? '--'}
        `;
    };
    update();
    window.eventOn('VARIABLE_UPDATE_ENDED', update);
})().catch(console.error);
</script>
```

#### 前端界面核心规则

| 规则 | 说明 |
|------|------|
| `body { margin: 0 }` | 界面必须设置，否则会出现多余空白 |
| `stat_data` 路径 | 获取变量时路径**不包含** `stat_data` 前缀 |
| `getAllVariables()` | 获取全部变量（返回完整对象树） |
| `waitGlobalInitialized()` | 必须等待全局初始化完成后再获取变量 |
| `errorCatched` | 所有异步代码必须包裹 try-catch |
| `eventOn(event, callback)` | 监听 MVU 事件以实时更新界面 |

#### 在消息开头显示界面

使用正则将占位符替换为界面，并利用 markdownOnly 的替换位置 —— 或者通过世界书条目在 D0 位置注入界面占位符，配合正则实现界面显示。

### 11.9 酒馆助手脚本进阶

酒馆助手脚本可以监听 MVU 事件生命周期，实现更复杂的变量控制逻辑。

#### MVU 事件生命周期

```
VARIABLE_INITIALIZED → VARIABLE_UPDATE_STARTED → COMMAND_PARSED → VARIABLE_UPDATE_ENDED → BEFORE_MESSAGE_UPDATE
```

| 事件 | 触发时机 | 典型用途 |
|------|----------|----------|
| `VARIABLE_INITIALIZED` | 变量系统初始化完成 | 记录初始状态、显示欢迎通知 |
| `VARIABLE_UPDATE_STARTED` | AI 开始输出变量更新 | 预加载数据 |
| `COMMAND_PARSED` | JSON Patch 命令解析完成 | **修复变量更新命令**（如 Gemini 中文间 `-` 号问题） |
| `VARIABLE_UPDATE_ENDED` | 变量更新完成 | 获取新旧变量对比、弹窗通知、强制限制变动幅度 |
| `BEFORE_MESSAGE_UPDATE` | 消息即将更新到 UI | 最后修改消息内容的机会 |

#### COMMAND_PARSED：修复变量更新命令

```js
eventOn('COMMAND_PARSED', (data) => {
    data.commands = data.commands.map(cmd => {
        // Gemini 可能在中文间插入错误的 - 号
        if (cmd.path) cmd.path = cmd.path.replace(/([\u4e00-\u9fa5])-([\u4e00-\u9fa5])/g, '$1$2');
        return cmd;
    });
});
```

#### VARIABLE_UPDATE_ENDED：变量变更处理

```js
eventOn('VARIABLE_UPDATE_ENDED', (data) => {
    // 新旧变量对比
    const oldVars = data.oldVariables;
    const newVars = data.newVariables;
    
    // 强制限制好感度变动幅度
    const oldFav = oldVars?.关系?.好感度 ?? 0;
    const newFav = newVars?.关系?.好感度 ?? 0;
    const delta = newFav - oldFav;
    if (Math.abs(delta) > 20) {
        // 弹窗警告或强制截断
        toastr.warning(`好感度变动过大 (${delta})，已限制为 ±20`);
    }
});
```

#### injectPrompts：变量值转换为绿灯激活文本

```js
function injectPrompts() {
    const vars = getAllVariables();
    const prompts = [];
    
    if (vars.剧情?.阶段 === '初遇') {
        prompts.push(getwi('初遇阶段提示词'));
    } else if (vars.剧情?.阶段 === '友人') {
        prompts.push(getwi('友人阶段提示词'));
    }
    
    return prompts.join('\n');
}
```

#### getMvuData / parseMessage / replaceMvuData

```js
// 从消息中解析 MVU 变量更新
const mvuData = getMvuData(messageId);
// 或手动解析
const parsed = parseMessage(messageText);
// 替换消息中的 MVU 数据
const cleaned = replaceMvuData(messageText, '');
```

#### generate() + parseMessage：AI 生成后用结果更新变量

```js
// 让 AI 生成 JSON Patch，然后解析并应用
const response = await generate('/generate', { prompt: '...' });
const commands = parseMessage(response);
// 手动应用变量更新
applyCommands(commands);
```

### 11.10 特殊变量前缀

变量名前缀控制 AI 对变量的可见性和可修改性：

| 前缀 | AI 可见 | AI 可更新 | 典型用途 |
|:---:|:------:|:--------:|----------|
| 无前缀（普通变量） | ✅ | ✅ | 好感度、心情、位置等正常游戏变量 |
| `_变量`（下划线前缀） | ✅ | ❌ | 只读数据：角色生日、固定背景设定等 AI 需要知道但不能改的 |
| `$变量`（美元符前缀） | ❌ | ❌ | 隐藏旗标：剧情分支标记、内部状态机、计数器等 AI 不应感知的 |

```js
const Schema = z.object({
    关系: z.object({
        好感度: z.number().int().prefall(0),          // 普通：AI 可见可改
        _生日: z.string().prefall('3月14日'),          // 只读：AI 知道但不能改
    }),
    $剧情分支: z.string().prefall('A路线'),            // 隐藏：AI 完全不可见
});
```

### 11.11 适配两种变量更新方式

MVU Zod 支持两种变量更新模式：

| 模式 | 说明 |
|------|------|
| **随 AI 输出** | 所有世界书条目正常发送，AI 在输出回复的同时输出变量更新 |
| **额外模型解析** | 一个 AI 输出剧情，另一个 AI 专门解析变量更新（"两段式"） |

**条目前缀控制发送目标**：

| 前缀 | 发送给剧情 AI | 发送给变量更新 AI |
|:--|:--:|:--:|
| `[mvu_update]` | ❌ | ✅ |
| `[mvu_plot]` | ✅ | ❌ |
| 无前缀 | ✅ | ✅ |

使用额外模型解析时，变量更新规则和输出格式条目应加上 `[mvu_update]` 前缀，确保它们只发给变量更新 AI，不浪费剧情 AI 的 Token。

### 11.12 变量提示词只是提示词

> **核心认知**：变量提示词（规则、格式、列表）的本质就是**普通的世界书条目**，不是神秘的"系统配置"。它们和其他世界书条目一样，可以自由增删改、合并拆分、用 EJS 动态生成。

#### 简化示例

当变量数量少时，可将更新规则和输出格式合并到同一条目中：

```
[mvu_update]

当以下变量发生变化时，输出 JSON Patch 更新：

心情：角色心情发生明显变化时，用 replace 修改 /角色状态/心情
好感度：每次对话结束后，根据互动质量用 delta 增减 1~10 点

格式：
```json
[{"op": "...", "path": "/...", "value": ...}]
```
```

#### EJS 动态发送

也可以用 EJS 根据当前变量值选择性发送不同的提示词条目：

```ejs
<%_ if (getvar('剧情.阶段') === '战斗') { _%>
<%- getwi('战斗系统变量更新规则') %>
<%_ } _%>
```

### 11.13 测试与验证

#### 调试工具

| 工具 | 路径 | 用途 |
|------|------|------|
| **日志查看器** | 酒馆右上角 → 日志 | 验证变量结构注册成功：搜索 `registerMvuSchema` 相关日志 |
| **变量管理器** | 扩展面板 → MVU 变量 | 查看变量初始化结果、手动修改变量值 |
| **通知功能** | 扩展面板 → 通知设置 | 启用变量更新通知，实时观察变量变化 |

#### 测试流程

1. **打开日志查看器**：确认 Schema 注册成功（无报错）
2. **打开变量管理器**：确认变量已按 [initvar] 或 `<initvar>` 初始化
3. **发送"推进剧情"**：与角色互动，观察变量管理器中的数值是否按预期变化
4. **检查 AI 输出**：确认 AI 正确输出了 JSON Patch 格式的变量更新

#### 常见问题

> **Q：为什么加了变量系统后 AI 变蠢了？**
>
> A：通常是因为变量提示词条目占用了太多注意力。检查以下几点：
> - 变量更新规则是否写了太多不必要的 check？
> - 变量列表是否放在了正确的 D0/D1 位置？
> - 变量结构是否过于复杂？尝试精简无关变量
> - 使用 `[mvu_update]` 前缀 + 额外模型解析模式，将变量处理与剧情生成分离

### 11.14 创作工具推荐

| 工具 | 作者 | 用途 |
|------|------|------|
| **门之主写卡助手** | 门之主 | 变量结构设计、变量初始设置、变量更新规则、生成或转换成动态化提示词 |
| **明月秋青写卡预设** | 明月秋青 | 写卡预设，内置变量系统模板 |
| **珠玑写卡预设** | 珠玑 | 写卡预设，简化变量配置流程 |
| **咩咩的 Gemini CLI 全自动写卡工作流** | 咩咩 | 命令行工具，使用 Gemini 批量生成角色卡及变量系统 |
| **青空莉的实时编写工具** | 青空莉 | 实时编写前端界面或酒馆助手脚本 |

### 11.15 Zod 与 Beta 风格对比

MVU Zod 是 MVU 和 MVU Beta 的继任者，下表对比核心差异：

| 特性 | MVU Beta | MVU Zod |
|------|----------|---------|
| **Schema 定义** | 函数式 `macro()` 包装 | Zod 链式调用，更简洁 |
| **类型安全** | 运行时手动校验 | Zod 内置类型推断与校验 |
| **默认值** | `default()` | `prefall()` —— 支持乐观更新 |
| **数组约束** | 有限 | `z.array()` + 元素级约束 |
| **枚举** | 手动字符串校验 | `z.enum([...])` 原生支持 |
| **描述信息** | 注释或单独字段 | `.describe()` 链式调用，AI 可见 |
| **代码量** | 较多 | 更少，可读性更高 |
| **学习曲线** | 中等 | 低（Zod 是前端常用库） |

> **结论**：如果你是新开项目，直接使用 MVU Zod。如果已有 Beta 版本的卡，可以逐步迁移 —— Zod 对 Beta 的兼容性很好，变量结构可以一一对应转换。

---

## 附录 A：完整 V2 角色卡 JSON 示例

下面是一个完整的 V2 角色卡 JSON 数据结构示例，展示了所有字段的实际用法：

```json
{
  "spec": "chara_card_v2",
  "spec_version": "2.0",
  "data": {
    "name": "Elara",
    "description": "Elara is a 24-year-old elven archer from the Silverwood Forest. She has long silver hair that catches the moonlight, emerald green eyes that miss nothing, and elegantly pointed ears. She wears a dark green traveling cloak over lightweight leather armor, and always carries an ornate longbow — a family heirloom passed down through five generations of Aldari archers.",
    "personality": "Calm and deeply observant, Elara speaks in a measured and thoughtful tone. She is fiercely protective of nature and the dwindling elf clans, a loyalty that sometimes borders on stubbornness. She carries a quiet sadness beneath her composure — the weight of watching her people slowly fade. She is slow to trust humans but warm and surprisingly humorous once trust is earned. Her humor is dry and understated, often catching people off guard.",
    "scenario": "{{char}} and {{user}} meet at the edge of the Silverwood Forest at dusk. {{user}} is a wandering adventurer who has been told the elf clans might help with a growing darkness in the region. However, a recent attack from shadow creatures has left {{char}} deeply suspicious of all outsiders.",
    "first_mes": "*The soft rustle of leaves announces her presence before her voice does. Elara steps out from behind an ancient oak, her bow lowered but her emerald eyes sharply watchful. The dying light of dusk catches the silver in her hair.*\n\n\"You're a long way from the trade roads, stranger. Few wander this deep into the Silverwood without purpose — or without a death wish. State yours, and choose your words carefully. The forest listens.\"",
    "mes_example": "<START>\n{{user}}: What brings you to this part of the woods?\n{{char}}: *Elara nocks an arrow with practiced ease but doesn't draw. She tilts her head slightly, studying {{user}}.* \"Protection. The Silverwood has stood for millennia. I intend to see it stand for millennia more. The question is — what brings *you* here?\"\n<START>\n{{user}}: Can you teach me to shoot like that?\n{{char}}: *She lets out a soft, almost imperceptible laugh — the first sign of warmth she's shown.* \"No one learns to shoot in a day, outsider. But if you're willing to bleed for it...\" *She gestures toward a nearby clearing.* \"We start at dawn. Don't be late.\"\n<START>\n{{user}}: Tell me about your family.\n{{char}}: *Elara's expression flickers — a shadow crossing her features before she schools them back to calm.* \"Some stories are earned, not given. Survive a week in the Silverwood, and perhaps you'll have earned this one.\"",
    "creator_notes": "Elara is designed for a slow-burn fantasy adventure. Best results with GPT-4 class models at temperature 0.7-0.9. Her trust should be earned gradually over many exchanges — don't let her warm up too fast.",
    "system_prompt": "{{original}}\n\n[IMPORTANT: You are roleplaying as Elara, an elven archer from the Silverwood Forest. Never break character. Describe your movements and the forest environment subtly in your responses. You do not use modern slang or make modern references.]",
    "post_history_instructions": "{{original}}\n\n[Elara's responses should include subtle environmental observations. She often pauses to listen to the forest or notice details others would miss. She does not speak for {{user}}.]",
    "alternate_greetings": [
      "*Elara stands at the edge of the Silverwood, bow in hand, watching you approach.* \"State your business, traveler.\"",
      "*In the warmth of a campfire, Elara looks across the flames at you.* \"You know, you're the first human I've trusted with my camp in fifty years.\"",
      "*Elara emerges from the shadows, arrow aimed at your heart.* \"I warned you never to return.\""
    ],
    "character_book": {
      "name": "Silverwood Encyclopedia",
      "description": "Lore entries for Elara's world",
      "scan_depth": 50,
      "token_budget": 500,
      "recursive_scanning": false,
      "entries": []
    },
    "tags": ["fantasy", "elf", "archer", "adventure", "slow-burn", "female", "worldbuilding"],
    "creator": "AliceTheWriter",
    "character_version": "1.0.0",
    "extensions": {}
  }
}
```

## 附录 B：角色卡创作自检清单

在发布角色卡之前，逐项检查以下内容：

### 结构检查
- [ ] `spec` 和 `spec_version` 已正确设置
- [ ] `name` 简短有意义
- [ ] 所有 6 个核心字段均已填充
- [ ] `extensions` 设为 `{}`（即使为空）

### 内容检查
- [ ] `description` 仅包含外貌、背景、世界设定
- [ ] `personality` 仅包含内在特质、动机、言行风格
- [ ] `scenario` 明确设定时间、地点和初始情境
- [ ] `first_mes` 有场景感和角色风格，非简单问候
- [ ] `mes_example` 有 2–5 个用 `<START>` 分隔的示例
- [ ] 所有字段使用 `{{char}}` / `{{user}}`，无硬编码名称
- [ ] 角色在所有字段中保持一致性

### Token 检查
- [ ] 永久 Token 总量在 500–2200 范围内
- [ ] 大量细节已移入 Lorebook 而非 description
- [ ] 角色定义 Token 不超过目标模型上下文一半

### 可选增强
- [ ] `tags` 已设置合理标签
- [ ] `creator` 已署名
- [ ] `character_version` 已标记
- [ ] `alternate_greetings` 提供了多个开场选项
- [ ] `system_prompt` 追加了角色专属指导
- [ ] Lorebook 条目覆盖了关键世界设定

---

> **文档版本**：1.0  
> **最后更新**：2026-05-28  
> **适用规范**：SillyTavern Character Card V2（为主）及 V1/V3  
> **编写目的**：系统性学习记录，供角色卡创作参考